<%= form_for(@test_case) do |f| %>
  <% if @test_case.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_case.errors.count, "error") %> prohibited this test_case from being saved:</h2>

      <ul>
      <% @test_case.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="show_requirements">
     <%= f.collection_check_boxes :requirement_ids, current_requirements,:id,:name %> 
</div>
<div class="show_steps">
<table>
   <tr>
      <th>description</th>
      <th>expected results </th>
   </tr>
  <%= f.fields_for :test_case_steps do |step| %>
  <tr>
    <td><%= step.text_field :description %> </td>
    <td><%= step.text_field :expected_result %></td>
  </tr>
  <% end %>
</table>
</div>




  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
